mixin FICON(title,text,icon,space)
    div(class=`flex flex-col gap-[4rem] feature`)
        div(class="flex flex-col justify-center items-center gap-[3rem]")
            img(src=icon, alt="")
            h3(class="flex md-font")  #{title}
        p(class="body-font text-center") #{text}